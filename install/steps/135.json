{
  "dbVersion": 135,
  "appVersion": "1.8.3",
  "steps": [
    {
      "step": "Create a new Table for remote Dataset settings",
      "action": "CREATE TABLE IF NOT EXISTS `datasetremote` (`rdId` bigint(20) NOT NULL AUTO_INCREMENT,`DataSetID` int(11) NOT NULL, `method` ENUM('GET','POST') NOT NULL, `uri` varchar(250) NOT NULL, `postData` TEXT DEFAULT NULL, `authentication` ENUM('none','plain','basic','digest') NOT NULL, `username` varchar(100) DEFAULT NULL, `password` varchar(250) DEFAULT NULL, `refreshRate` int(11) DEFAULT 86400, `clearRate` int(11) DEFAULT 0, `runsAfter` int(11) DEFAULT NULL, `dataRoot` varchar(250) DEFAULT NULL, `lastSync` int(11) NOT NULL DEFAULT 0, `summarize` varchar(10) DEFAULT NULL, `summarizeField` varchar(250) DEFAULT NULL, PRIMARY KEY (`rdId`)) ENGINE=InnoDB  DEFAULT CHARSET=utf8 AUTO_INCREMENT=1;"
    },
    {
      "step": "Create a new ColumnType",
      "action": "INSERT INTO `datasetcolumntype` SET dataSetColumnTypeId=3, dataSetColumnType='Remote';"
    },
    {
      "step": "Create a new Column in DataSetColumn",
      "action": "ALTER TABLE `datasetcolumn` ADD `RemoteField` VARCHAR(250) DEFAULT NULL AFTER `formula`;"
    },
    {
      "step": "Create a Task for fetching all remote DataSets automatically",
      "action": "INSERT INTO `task` SET `name`='Fetch Remote DataSets', `class`='\\Xibo\\XTR\\RemoteDataSetFetch', `status`=2, `isActive`=1, `configFile`='/tasks/remote-dataset.task', `options`='{}', `schedule`='30 * * * * *';"
    }
  ]
}