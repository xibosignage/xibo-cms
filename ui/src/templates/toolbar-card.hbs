<div class="toolbar-card {{#if favourited}}favourited{{/if}}" 
    data-toggle="tooltip" 
    data-type="module" 
    data-sub-type="{{type}}"
    data-region-specific="{{regionSpecific}}"
    data-valid-ext="{{ validExtensions }}"
    data-max-size="{{ maxSize }}" 
    data-max-size-message="{{ maxSizeMessage }}"
    data-title="{{description}}"
    drop-to="region">
    
    <div class="btn-favourite" data-toggle="tooltip" data-placement="left" 
        {{#unless favourited}}
            data-title="{{trans.addAsFavourite}}
        {{/unless}}
        ">
        <i class="{{#if favourited}}fas{{else}}far{{/if}} fa-star"></i>
    </div>

    <div class="add-area action-area" data-title="{{trans.add}}" data-toggle="tooltip" data-placement="left">
        <i class="fa fa-plus"></i>
    </div>
    <div class="drag-area action-area" data-title="{{trans.grab}}" data-toggle="tooltip" data-placement="right">
        <i class="fa fa-hand-rock-o"></i>
    </div>

    <i class="fa module-icon-{{type}} card-icon"></i>
    
    <span class="card-title">{{name}}</span>
</div>