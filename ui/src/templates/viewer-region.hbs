<div id="{{id}}" data-type="region" class="designer-region tooltip-always-on {{#if isEditable}}editable{{/if}} {{#if isEmpty}}empty{{/if}}" data-region-id="{{id}}" data-toggle="tooltip" data-container="#layout-viewer" data-title="{{name}}" style="position:absolute; 
        width: {{ scaledDimensions.width }}px; 
        height: {{ scaledDimensions.height }}px; 
        top: {{ scaledDimensions.top }}px;
        left: {{ scaledDimensions.left }}px;
        z-index: {{ zIndex }};">

        <div class="designer-region-overlay"></div>
</div>