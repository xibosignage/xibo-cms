<div id="properties-panel-container">

    <div class="form-title form-title-{{style}}">
        {{header}}
    </div>

    <div class="row button-container">
        {{#each buttons}}
            <button type="button" onclick="{{this}}" class="btn btn-info">
                {{@key}}
            </button>
        {{/each}}
    </div>

    {{{form}}}

</div>