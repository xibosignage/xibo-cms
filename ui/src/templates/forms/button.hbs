<button type="button" id="{{id}}" data-action="{{action}}" data-sub-action="{{subAction}}" {{#if click}}onclick="{{click}}"{{/if}} class="properties-panel-btn xibo-form-btn btn {{type}}">{{name}}</button>