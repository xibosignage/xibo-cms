<nav class="navbar viewer-navbar designer-{{element.type}}-selected">
    {{! Left region }}
    <div class="pull-left">
        {{#if renderLayout}}
            <button type="button" id="play-btn" class="btn btn-sm" data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top" data-title="{{ trans.playPreviewLayout }}">
                <i class="fa fa-play-circle" data-toggle="popover" title="{{ trans.playPreviewLayoutPOTitle }}" data-content="{{ trans.playPreviewLayoutPOMessage }}" data-placement="top"></i>
            </button>
        {{else}}
            <button type="button" id="inline-editor-save" class="btn btn-sm" data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top"
                data-title="{{ trans.saveEditorChanges }}">
                <i class="fa fa-save"></i>
            </button>
        {{/if}}

        {{#unless readOnlyModeOn}}
            {{#if element.isPermissionsModifiable}}
                <button type="button" id="permissions-btn" class="properties-btn btn btn-sm" data-property="Permissions" 
                    data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top" data-title="{{trans.tools.permissions.description}}">
                    <i class="fa tool-icon-permissions card-icon"></i>
                </button>
            {{/if}}

            {{#eq element.type "widget"}}
                {{#if element.isEditable}}
                    {{#if element.canAttachAudio}}
                        <button type="button" id="audio-btn" class="properties-btn btn btn-sm" data-property="Audio" 
                            data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top" data-title="{{trans.tools.audio.description}}">
                            <i class="fa tool-icon-audio card-icon"></i>
                        </button>
                    {{/if}}

                    <button type="button" id="expiry-btn" class="properties-btn btn btn-sm" data-property="Expiry" 
                        data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top" data-title="{{trans.tools.expiry.description}}">
                        <i class="fa tool-icon-expiry card-icon"></i>
                    </button>

                    <button type="button" id="transitionIn-btn" class="properties-btn btn btn-sm" data-property="Transition" data-property-type="in" 
                        data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top" data-title="{{trans.tools.transitionIn.description}}">
                        <i class="fa tool-icon-transitionIn card-icon"></i>
                    </button>

                    <button type="button" id="transitionOut-btn" class="properties-btn btn btn-sm" data-property="Transition" data-property-type="out" 
                        data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top" data-title="{{trans.tools.transitionOut.description}}">
                        <i class="fa tool-icon-transitionOut card-icon"></i>
                    </button>
                {{/if}}
            {{/eq}}
        {{/unless}}
    </div>

    {{! Right region }}
    <div class="pull-right">
        {{#unless readOnlyModeOn}}
            {{#if undoActive}}
                <button type="button" id="undo-btn" class="btn btn-sm" data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top"
                    data-title="{{trans.undoActiveTitle}}">
                    <i class="fa fa-undo"></i>
                </button>
            {{/if}}

            {{#if trashActive}}
                <button type="button" id="delete-btn" class="btn btn-sm" data-toggle="tooltip" data-container=".editor-bottom-bar" data-placement="top"
                    data-title="{{trans.trashBinActiveTitle}}">
                    <i class="fa fa-trash"></i>
                </button>
            {{/if}}
        {{/unless}}
    </div>

    <div class="viewer-navbar-overlay">
        {{! Element Info }}
        {{#eq element.type "widget"}}
            <div class="info d-inline">
                {{#if regionName}}
                    <span class="label-name align-middle d-inline-block">
                        "{{regionName}}"
                    </span>
                    {{#ifCond element.widgetName '||' element.moduleName}}
                        <i class="fas fa-arrow-right align-middle"></i>
                    {{/ifCond}}
                {{/if}}
                {{#ifCond element.widgetName '||' element.moduleName}}
                    <span class="label-name align-middle d-inline-block">
                        {{#if element.widgetName}}"{{element.widgetName}}"{{/if}} {{#if element.moduleName}}({{element.moduleName}}){{/if}}
                    </span>
                {{/ifCond}}
            </div>
        {{else eq element.type "element"}}
            <div class="info d-inline">
                <span class="label-name align-middle" title="{{element.name}}">
                    {{element.elementType}} - {{element.id}} ({{element.type}})
                </span>
            </div>
        {{else}}
            <div class="info d-inline">
                <span class="label-name align-middle" title="{{element.name}}">
                    "{{element.name}}" ({{element.type}})
                </span>
            </div>
        {{/eq}}
    </div>
</nav>