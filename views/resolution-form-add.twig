{#
/*
 * Spring Signage Ltd - http://www.springsignage.com
 * Copyright (C) 2015 Spring Signage Ltd
 * (${FILE_NAME})
 */
$formFields[] = Form::AddText('resolution', __('Resolution'), NULL,
            __('A name for this Resolution'), 'r', 'required');

        $formFields[] = Form::AddNumber('width', __('Width'), NULL,
            __('The Width for this Resolution'), 'w', 'required');

        $formFields[] = Form::AddNumber('height', __('Height'), NULL,
            __('The Height for this Resolution'), 'h', 'required');
#}

{% extends "form-base.twig" %}
{% import "forms.twig" as forms %}

{% block formTitle %}
    {% trans "Add Resolution" %}
{% endblock %}

{% block formButtons %}
    {% trans "Save" %}, $("#resolutionAddForm").submit()
    {% trans "Cancel" %}, XiboDialogClose()
    {% trans "Help" %}, XiboHelpRender("{{ help }}")
{% endblock %}

{% block formHtml %}
    <div class="row">
        <div class="col-md-12">
            <form id="resolutionAddForm" class="XiboForm form-horizontal" method="post" action="{{ urlFor("resolution.add") }}">
                        {% set title %}{% trans "Resolution" %}{% endset %}
                        {% set helpText %}{% trans "A name for this Resolution" %}{% endset %}
                        {{ forms.text("resolution", title, "", helpText) }}
                        
                        {% set title %}{% trans "Width" %}{% endset %}
                        {% set helpText %}{% trans "The Width for this Resolution" %}{% endset %}
                        {{ forms.text("width", title, "", helpText) }}
                       
                        {% set title %}{% trans "Height" %}{% endset %}
                        {% set helpText %}{% trans "The Height for this Resolution" %}{% endset %}
                        {{ forms.text("height", title, "", helpText) }}
            </form>
        </div>
    </div>
{% endblock %}