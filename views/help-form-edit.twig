{#
/*
 * Spring Signage Ltd - http://www.springsignage.com
 * Copyright (C) 2015 Spring Signage Ltd
 * (${FILE_NAME})
 */
$formFields[] = Form::AddText('Topic', __('Topic'), Sanitize::string($row['Topic']),
            __('The Topic for this Help Link'), 't', 'maxlength="254" required');

        $formFields[] = Form::AddText('Category', __('Category'), Sanitize::string($row['Category']),
            __('The Category for this Help Link'), 'c', 'maxlength="254" required');

        $formFields[] = Form::AddText('Link', __('Link'), Sanitize::string($row['Link']),
            __('The Link to open for this help topic and category'), 'c', 'maxlength="254" required');
#}

{% extends "form-base.twig" %}
{% import "forms.twig" as forms %}

{% block formTitle %}
    {% trans "Edit Help Link" %}
{% endblock %}

{% block formButtons %}
    {% trans "Save" %}, $("#helpEditForm").submit()
    {% trans "Cancel" %}, XiboDialogClose()
    {% trans "Help" %}, XiboHelpRender("{{ help }}")
{% endblock %}

{% block formHtml %}
    <div class="row">
        <div class="col-md-12">
            <form id="helpEditForm" class="XiboForm form-horizontal" method="put" action="{{ urlFor("help.edit", {id: help.helpId}) }}">
                    {% set title %}{% trans "Topic" %}{% endset %}
                    {% set helpText %}{% trans "The Topic for this Help Link" %}{% endset %}
                    {{ forms.text("topic", title, help.topic, helpText) }}
                        
                    {% set title %}{% trans "Category" %}{% endset %}
                    {% set helpText %}{% trans "he Category for this Help Link" %}{% endset %}
                    {{ forms.text("category", title, help.category, helpText) }}
                       
                    {% set title %}{% trans "Link" %}{% endset %}
                    {% set helpText %}{% trans "The Link to open for this help topic and category" %}{% endset %}
                    {{ forms.text("link", title, help.link, helpText) }}
            </form>
        </div>
    </div>
{% endblock %}