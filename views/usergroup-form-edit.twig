{#
/*
 * Spring Signage Ltd - http://www.springsignage.com
 * Copyright (C) 2015 Spring Signage Ltd
 * (${FILE_NAME})
 */

#}

{% extends "form-base.twig" %}
{% import "forms.twig" as forms %}

{% block formTitle %}
    {% trans "Edit User Group" %}
{% endblock %}

{% block formButtons %}
    {% trans "Help" %}, XiboHelpRender("{{ help.edit }}")
    {% trans "Cancel" %}, XiboDialogClose()
    {% trans "Save" %}, $("#userGroupEditForm").submit()
{% endblock %}

{% block formHtml %}
    <div class="row">
        <div class="col-md-12">
            <form id="userGroupEditForm" class="XiboForm form-horizontal" method="put" action="{{ urlFor("group.edit", {"id": group.groupId}) }}">
                {% set title %}{% trans "Name" %}{% endset %}
                {% set helpText %}{% trans "The Name for this User Group" %}{% endset %}
                {{ forms.text("group", title, group.group, helpText, "", "required maxlength='50'") }}

                {% set title %}{% trans "Library Quota" %}{% endset %}
                {% set helpText %}{% trans "The quota in Kb that should be applied. Enter 0 for no quota." %}{% endset %}
                {{ forms.number("libraryQuota", title, group.libraryQuota, helpText) }}
            </form>
        </div>
    </div>
{% endblock %}